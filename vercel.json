{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm db:generate && node scripts/vercel-production-db-prepare.mjs && pnpm --filter @sinhon-os/api build && pnpm --filter @sinhon-os/web build",
  "functions": {
    "api/index.js": {
      "maxDuration": 30
    }
  },
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": "apps/web/.next",
  "rewrites": [
    {
      "source": "/api/v1/:path*",
      "destination": "/api/index.js"
    }
  ]
}
