{
  "name": "sinhon-life-api",
  "version": "0.1.0",
  "private": true,
  "description": "sinhon.life — 인스타 비즈 게시물 → D1 미러링 + 공개 미디어 API",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dev": "wrangler deploy --env dev",
    "db:init": "wrangler d1 execute sinhon-life-media --file migrations/0001_init.sql --remote && wrangler d1 execute sinhon-life-media --file migrations/0002_auth.sql --remote",
    "db:init:local": "wrangler d1 execute sinhon-life-media --file migrations/0001_init.sql --local && wrangler d1 execute sinhon-life-media --file migrations/0002_auth.sql --local",
    "db:init:dev": "wrangler d1 execute sinhon-life-media-dev --file migrations/0001_init.sql --remote && wrangler d1 execute sinhon-life-media-dev --file migrations/0002_auth.sql --remote",
    "tail": "wrangler tail",
    "secret:token": "wrangler secret put IG_ACCESS_TOKEN",
    "secret:userid": "wrangler secret put IG_USER_ID",
    "poll:manual": "echo 'curl YOUR_WORKER_URL/_admin/poll?key=ADMIN_KEY  # 수동 폴링 트리거'",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260501.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.80.0"
  }
}
