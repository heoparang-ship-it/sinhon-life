# 신혼OS AGENTS.md

## 1. 프로젝트 목적

신혼OS는 예비 신혼부부와 혼인 초기 부부가 예산, 주거, 정책 혜택, 파트너 상품 비교, 공동 승인, 상담 신청까지 한곳에서 함께 결정하도록 돕는 커플 중심 실행 서비스다.

이 프로젝트는 단순 예식 정보 앱이 아니다. 핵심은 두 사람이 같은 기준으로 비용과 선택지를 보고, 출처가 있는 정책과 검증된 가격 정보를 바탕으로 다음 행동을 정하는 것이다.

## 2. 핵심 사용자

- 예비 신혼부부: 결혼 준비, 예산 계획, 주거 계획, 업체 비교가 필요한 커플
- 혼인 5년 이내 부부: 주거, 정책 혜택, 생활 비용, 가족 계획 관련 결정을 이어가는 커플
- 파트너 업체: 웨딩, 주거, 가전, 가구, 금융 외 비금융 상담 등 검증된 상품과 상담을 제공하는 업체
- 운영자: 정책 데이터, 파트너 데이터, 가격 검증, 리드 상태, 콘텐츠, 품질을 관리하는 내부 사용자

## 3. 제품 원칙

- 개인보다 커플 중심으로 설계한다. 중요한 결정은 두 사람의 상태, 승인, 기록을 기준으로 다룬다.
- 가격은 항목별 최저가보다 커플이 실제로 예상할 총액을 우선한다.
- 정책과 혜택 정보는 출처 URL, 기준일, 검증일, 룰 버전을 반드시 가진다.
- 파트너 가격은 검증일, 유효기간, 포함 항목, 필수 옵션, 선택 옵션을 분리해서 표시한다.
- 유효기간이 지난 가격은 기본 노출하지 않는다. 필요한 경우 만료 상태를 분명히 표시한다.
- 개인정보는 최소 수집한다. 소득, 자산, 예산 등 민감한 값은 가능한 한 정확한 금액이 아니라 구간으로 받는다.
- 금융 심사, 대출 승인, 정책 선정 가능성을 단정하지 않는다.
- "최저가", "무조건 가능", "확정", "보장"처럼 오해를 만들 수 있는 표현을 피한다.
- 비즈니스 규칙은 추측하지 않는다. 불명확한 규칙은 문서에 "확정 필요"로 남긴다.
- 모바일 화면을 기본 사용 환경으로 본다.

## 4. 기술 원칙

- TypeScript strict 모드를 유지한다.
- 핵심 기능은 테스트 없이 병합하지 않는다.
- API 응답 타입과 에러 타입을 명확히 정의한다.
- 로딩, 에러, 빈 상태를 모든 주요 화면에 포함한다.
- 접근성을 기본 요구사항으로 본다. 버튼 이름, 폼 라벨, 키보드 탐색, 색상 대비를 확인한다.
- 한국어 문구는 쉬운 표현을 사용하고, 긴 문장이 모바일에서 깨지지 않게 한다.
- 관련 없는 리팩터링을 하지 않는다.
- 한 PR은 하나의 목적만 가진다.
- 문서와 구현이 충돌하면 구현을 바꾸기 전에 먼저 문서의 최신성을 확인한다.
- 운영, 정책, 결제, 개인정보, 파트너 정산과 관련된 변경은 사람이 반드시 리뷰한다.

## 5. 보안 원칙

- 민감정보를 로그, 분석 이벤트, 에러 메시지, URL 쿼리에 남기지 않는다.
- 전화번호, 이름, 파일명, 계좌, 주민등록번호 등 직접 식별 정보는 저장과 노출을 최소화한다.
- 주민등록번호 입력 기능은 만들지 않는다.
- 커플 데이터 접근은 CoupleMember 권한을 기준으로 제한한다.
- 초대 링크는 만료 시간과 재사용 제한을 가져야 한다.
- 관리자와 파트너 권한은 명확히 분리한다.
- 리드 전송은 개인정보 제공 동의 후에만 가능하다.
- 파트너에게 노출되는 개인정보는 필요한 범위로 제한하고 기본적으로 마스킹한다.
- 문서 파일은 접근 권한을 반드시 확인한다.
- 결제 기능은 별도 설계, 법무, 보안 검토 후에만 구현한다.

## 6. 데이터 모델 원칙

- User가 아니라 Couple을 핵심 단위로 본다.
- User는 여러 기능의 행위자지만, 중요한 신혼 준비 데이터는 Couple에 귀속된다.
- CoupleMember는 사용자와 커플의 관계, 역할, 초대 상태, 참여 상태를 표현한다.
- 정책 룰과 가격 정보는 버전 관리한다.
- 사용자의 주요 결정은 DecisionLog로 남긴다.
- 운영상 중요한 이벤트는 EventLog로 남기되 개인정보를 payload에 저장하지 않는다.
- 삭제 정책은 도메인별로 명시한다. 사용자 경험, 감사 기록, 개인정보 삭제 요구를 함께 고려한다.
- 개인정보 컬럼과 암호화 필요 컬럼은 DB 설계 문서에 따로 표시한다.

## 7. PR 작성 규칙

모든 PR은 아래 항목을 포함한다.

- 목적: 이 PR이 해결하는 문제와 범위
- 변경 파일: 주요 변경 파일과 이유
- 테스트 결과: 실행한 lint, typecheck, test, build 결과
- 남은 리스크: 확정 필요 사항, 미구현 범위, 운영상 주의점
- 다음 작업 제안: 이 PR 이후 자연스럽게 이어질 작은 작업

PR 작성 시 지켜야 할 기준:

- 한 PR은 하나의 목적만 가진다.
- 범위 밖 파일을 수정하지 않는다.
- 정책, 결제, 개인정보, 파트너 정산 변경은 사람 리뷰를 필수로 표시한다.
- 구현 중 비즈니스 규칙이 불명확하면 임의로 정하지 말고 TODO 또는 "확정 필요"로 남긴다.

## 8. 테스트 작성 규칙

- 핵심 도메인 로직은 단위 테스트를 작성한다.
- 사용자 흐름은 필요한 경우 E2E 테스트로 검증한다.
- API는 성공, 권한 없음, 입력 오류, 존재하지 않는 리소스, 상태 전이 오류를 테스트한다.
- 커플 권한, 배우자 초대, 공동 승인, 리드 생성, 정책 매칭, 가격 버전은 테스트 우선순위가 높다.
- 분석 이벤트는 개인정보가 payload에 들어가지 않는지 테스트한다.
- UI는 로딩, 에러, 빈 상태를 확인한다.
- 모바일 viewport에서 핵심 흐름을 검증한다.

## 9. Codex가 절대 추측하면 안 되는 영역

- 정책 자격 조건과 최신 수치
- 금리, 대출 가능 여부, 한도, 승인 가능성
- 파트너 실제 가격, 할인, 포함 항목, 필수 옵션
- 취소, 환불, 위약금, 정산 규칙
- 개인정보 보관 기간과 파기 기준
- 법무 검토가 필요한 문구
- 결제 상태 전이와 PG 연동 방식
- 운영자, 파트너, 커플 간 권한 경계
- 커플 데이터 삭제와 감사 로그 보존 정책

불명확한 사항은 다음 표현 중 하나로 남긴다.

- 확정 필요
- 운영 정책 확정 필요
- 법무 검토 필요
- 보안 검토 필요
- 파트너 계약 기준 확정 필요

## 10. 작업 전 반드시 확인할 문서

작업자는 변경 전 아래 문서를 확인한다. 아직 없는 문서는 먼저 작성하거나, 현재 작업 범위에서 "문서 없음"을 명시한다.

- AGENTS.md
- docs/01-prd.md
- docs/02-user-flows.md
- docs/03-domain-model.md
- docs/04-database-schema-plan.md
- docs/05-api-contract.md
- 관련 기능 문서
- 신혼OS*코덱스*명령세트\_v1.md

## 11. Codex 작업 방식

- 코드 작업은 먼저 plan only로 제출한다.
- plan only에는 수정할 파일, 영향 범위, 데이터 모델 변경 여부, 테스트 계획을 포함한다.
- 사용자가 승인하기 전에는 파일을 수정하지 않는다.
- 승인 후 구현하면 lint, typecheck, test, build 결과를 보고한다.
- 문서만 작성하는 단계에서도 변경 파일과 남은 리스크를 보고한다.
- 작업은 명령세트의 순서를 따른다.
