version: 1
applications:
  - appRoot: apps/web
    frontend:
      phases:
        preBuild:
          commands:
            - npm install -g pnpm@11.5.1
            - cd "$CODEBUILD_SRC_DIR" && pnpm install --frozen-lockfile
        build:
          commands:
            - cd "$CODEBUILD_SRC_DIR" && npm --prefix apps/web run build
      artifacts:
        baseDirectory: .next
        files:
          - "**/*"
      cache:
        paths:
          - ../../node_modules/**/*
          - ../../apps/web/.next/cache/**/*
